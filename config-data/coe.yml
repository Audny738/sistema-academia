server:
  port: ${random.value}
  servlet:
    context-path: /coe
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/uady
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
